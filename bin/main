#!/usr/bin/env ruby

require 'gtfs'
require "thor"

class GtfsCLI < Thor
  desc "update_database", "Update the database using GTFS"

  def update_database
    data = Gtfs::GtfsData.new
    data.import_data
  end
end

GtfsCLI.start(ARGV)
